using System.Collections.Generic;
using UnityEngine;

public class AgentDiagnostics : MonoBehaviour
{
    /*[SerializeField] bool validate;
    [SerializeField] GridWorldAgent agent;
    [SerializeField] Vector3 currentPosition;
    [SerializeField] Vector3 priorPosition;

    void OnValidate()
    {
        if (!validate) return;
        currentPosition = agent.body.position;
        priorPosition = agent.body.priorPosition;
    }*/
}
